services:
  - type: web
    name: taiwan-artion
    env: static
    buildCommand: npm run build
    staticPublishPath: ./dist
    redirects:
      - source: /*
        destination: /index.html
        statusCode: 200
